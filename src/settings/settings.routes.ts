import { NAV_SETTINGS_PASSWORD } from './settings.links'
import SettingsPassword from './pages/Password/Password'

// eslint-disable-next-line import/prefer-default-export
export const settingsRoutes = [
  {
    path: NAV_SETTINGS_PASSWORD,
    component: SettingsPassword,
  },
]
